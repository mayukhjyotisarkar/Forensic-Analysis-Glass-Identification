pandas>=2.0
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4
imbalanced-learn>=0.12
joblib>=1.3
streamlit>=1.35      # only needed for app.py
jupyter              # only needed to run the notebooks
pytest               # only needed to run the tests
shap>=0.40.0         # explainability support
